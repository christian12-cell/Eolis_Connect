[build]
builder = "nixpacks"
buildCommand = "pip install -r eolis-api/requirements.txt"

[deploy]
startCommand = "cd eolis-api && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
